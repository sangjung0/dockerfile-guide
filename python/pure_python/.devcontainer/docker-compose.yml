name: Python
services:
    dev:
        build:
            context: ..
            dockerfile: .devcontainer/Dockerfile
        volumes:
            - ../:/workspaces/dev
            - /workspaces/dev/.venv
        command: ["tail", "-f", "/dev/null"]
